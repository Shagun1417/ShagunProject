//Update the above trigger to delete the Last Name from the Account field when a Contact is deleted without deleteing the other Last name.
trigger TiggerSheet_1_5 on Account (after insert) {
   
   
}